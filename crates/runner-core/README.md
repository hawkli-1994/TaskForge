# taskforge-runner-core

Core library for the TaskForge local runner. It provides configuration,
platform client, authentication, event spooling, agent host abstractions,
and redaction utilities used by the `taskforge-runner` CLI.

## Build & Test

```bash
cargo check -p taskforge-runner-core
cargo test -p taskforge-runner-core
```
